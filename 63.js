// write a program to create a new string from middle letters of string

function creates(){
    var  str = prompt();
    var max = Math.floor(str.length/2);
    console.log(str.slice(max-1,max+2));
    console.log(max);

     
}

creates();

// done 

