// write a program to extract first half of string of even length

function extract(){
     var str = 'ronitraj';
     var half = (str.length)/2;
     console.log(str.slice(0,half));
}

extract();

// done 