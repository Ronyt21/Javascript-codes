// write a program to check whether given number is lie between 
// the range of 40..10000

function check(){
    var num = parseInt(prompt());
    if(num >= 40 && num <= 10000){
        console.log('true');

    }
    else{
        console.log('false');
    }
}
check();

// done 