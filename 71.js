// write a program to find whether the first and last elemnt of array is 1 or not 


function check(){
    var arr = [2,3,4,5];
   if(arr[0] === 1 || arr[arr.length] === 1){
     console.log('true !!');
     
   }
   else{
       console.log('false !!')
   }
}

check();

// done 